{
  "name": "read-git",
  "description": "A smart way to use Github",
  "homepage": "https://readgit.herokuapp.com/",
  "private": false,
  "scripts": {
    "build": "ionic-app-scripts build --release && npm run octokat",
    "watch": "ionic-app-scripts watch",
    "serve:before": "watch",
    "emulate:before": "build",
    "deploy:before": "build",
    "build:before": "build",
    "run:before": "build",
    "lint": "ionic-app-scripts test",
    "octokat": "browserify github.js -o www/build/github.js",
    "heroku-postbuild": "npm run build && gzip -k -r --best --force www",
    "livereload": "ionic run android --livereload --address 192.168.137.1 --device",
    "android": "cordova run android --buildConfig=.vscode/build.json",
    "release": "npm run build && npm run android -- --release",
    "deeplink": "adb shell am start -a android.intent.action.VIEW -d 'github://github.com/prijindal/readGit'"
  },
  "dependencies": {
    "@ionic/storage": "^1.0.3",
    "ionic-angular": "^2.0.0-rc.0",
    "ionic-native": "^2.0.3",
    "ionicons": "^3.0.0",
    "moment": "^2.15.1",
    "octokat": "^0.4.18",
    "x2js": "^3.0.0"
  },
  "devDependencies": {
    "@ionic/app-scripts": "^0.0.23",
    "browserify": "^13.1.0",
    "node-static": "^0.7.8",
    "typescript": "^2.0.3"
  },
  "cordovaPlugins": [
    "cordova-plugin-device",
    "cordova-plugin-console",
    "cordova-plugin-whitelist",
    "cordova-plugin-splashscreen",
    "cordova-plugin-statusbar",
    "ionic-plugin-keyboard"
  ],
  "cordovaPlatforms": []
}
